# BCTChain DEX Environment Configuration

# Cloudflare R2 Configuration
# Replace with your actual R2 public URL
NEXT_PUBLIC_TOKEN_LIST_URL=https://pub-65a77754903f4bddb48f3852b3967938.r2.dev/bctchain-token-list.json

# Alternative: Custom domain if you've set up a custom domain for R2
# NEXT_PUBLIC_TOKEN_LIST_URL=https://tokens.bctchain.com/bctchain-token-list.json

# Development mode (set to 'development' to use local token list)
NODE_ENV=production

# Optional: Token list cache duration in milliseconds (default: 300000 = 5 minutes)
NEXT_PUBLIC_TOKEN_LIST_CACHE_DURATION=300000

# Optional: Enable debug logging for token list operations
NEXT_PUBLIC_TOKEN_LIST_DEBUG=false
